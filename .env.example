# Docker Compose project name
COMPOSE_PROJECT_NAME=agentmemory

# Host path mounted to /data in the container.
# Relative paths resolve from the docker/ directory.
# Production: use an absolute path, e.g. /opt/agentmemory/data
AGENTMEMORY_DATA_DIR=../data
